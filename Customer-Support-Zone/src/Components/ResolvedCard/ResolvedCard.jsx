import React from 'react';

const ResolvedCard = ({card, resolvedTask, setresolvedTask}) => {
    return (
        <div>
          <p>{card.title}</p>
        </div>
    );
};

export default ResolvedCard;